// google-stubs: packages the WireMock JSON stub mappings as a resources jar so the
// backend can serve them from the classpath when the `stub-google` profile is active.
plugins {
    `java-library`
}

dependencies {
}

java {
    sourceCompatibility = JavaVersion.VERSION_17
}
