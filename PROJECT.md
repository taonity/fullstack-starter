# Project Context

> This file defines what the project is and what to build next. `AGENTS.md` defines how (architecture, conventions, build commands). Read both before any implementation task.
> When filling in sections below, write plainly — then refine each answer into clear, specific language that an LLM can act on directly.
> If any section is vague or incomplete, ask clarifying questions and suggest improvements before proceeding.

---

## 1. Vision

<!--
What to write here:
- What the product IS (one sentence)
- Who uses it and why
- What the finished product looks like (key screens, core workflow, scale)
- Hard constraints: tech stack locks, compliance, third-party integrations, team size, timeline

Example (delete before filling in):
"Internal shift-scheduling tool for a 200-person restaurant chain. Managers build weekly schedules,
employees swap shifts via mobile. Integrates with ADP payroll export. Must be WCAG 2.1 AA accessible.
Solo developer, targeting MVP in 8 weeks."
-->

_[Replace this line with your vision]_

## 2. First Step

<!--
What to write here:
- The single feature or component to build FIRST
- What data it reads/writes (entities, fields)
- What the user sees and does (UI or API surface)
- Acceptance criteria: how you know it's done

Example (delete before filling in):
"Add a 'Locations' CRUD feature. Entity: Location(id, name, address, timezone, maxCapacity).
REST endpoints: GET/POST/PUT/DELETE /api/locations. Frontend: table listing all locations with
an 'Add Location' modal form. Done when: locations persist across restarts, table loads under 200ms
for 500 rows, and form validates required fields client-side."
-->

_[Replace this line with your first step]_

---

## Progress

### Done
- [x] Fork from fullstack-starter
- [ ] Vision defined
- [ ] First step defined

### Backlog
<!-- Add future features here as they emerge during development. Check off items as they're completed. -->

